import 'package:flutter/material.dart';

class CustomizePanelScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Customize Panel')),
      body: Center(child: Text('Customize Panel Screen')),
    );
  }
}
