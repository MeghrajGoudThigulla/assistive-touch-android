import 'package:flutter/material.dart';

class CustomizeIconScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Customize Icon')),
      body: Center(child: Text('Customize Icon Screen')),
    );
  }
}
